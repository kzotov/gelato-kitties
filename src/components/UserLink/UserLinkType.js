import * as PropTypes from 'prop-types';

const UserLinkType = {
  address: PropTypes.string,
  image: PropTypes.string,
  nickname: PropTypes.string,
  hasDapper: PropTypes.bool,
};

export default UserLinkType;